# 素材来源

| 素材 | 来源与处理 |
|---|---|
| 中南大学 Logo | 用户指定 `D:\Users\86187\Desktop\game\logo.webp`；保存到 `Assets\Source\CSU_logo.webp`；按比例缩放完整展示 |
| 迪文 Logo | 用户指定 `D:\Users\86187\Desktop\game\迪文logo.jpg`；保存到 `Assets\Source\DWIN_logo.jpg`；保留原比例 |
| 四段蜂鸣器配乐 | 本项目 `Tools\build_audio.py` 原创旋律与程序合成；针对 EKT043E 板载蜂鸣器生成 2.1—2.8 kHz 单音方波，无外部音乐或采样；WAV 源文件及 WAE 均保留 |
| 角色、怪物、门、宝箱等像素图 | [Kenney Tiny Dungeon](https://kenney.nl/assets/tiny-dungeon)，CC0；原压缩包、PNG 和 License.txt 均保留在 `Assets\Source` |
| 地板、钥匙、宝石、楼梯、闪光、方向符号 | 本项目以绘图代码生成 |
| 页面排版、边框、按钮、文字、启动动画 | 本项目 `Tools\build_art.py` 生成，800×480 |
| 中文文字 | 本机微软雅黑字体栅格化为图片；工程不附带微软字体文件 |
| 前期画风参考 | `Assets\Source\approved_style.png` 为前期生成的视觉参考，不作为完整游戏截图 |

Kenney 原始图块为 16×16，按最近邻方式放大成 32×32 地图元素；头像放大为 96×96。部分图块进行颜色调整以区分门和道具。文件编号及用途可在 `build_art.py` 查到。

双 Logo 开机画面不再使用旧校徽裁图。动画为 15 帧、每帧约 200 ms，透明渐入与加载条推进，由 C51 切换页面 11—25 播放；返回首页与换层复用这些帧，不重复占用图片 Flash。页面 10 为动画结束后的账号密码登录页。
